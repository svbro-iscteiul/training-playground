pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
        stage('build') {
            steps {
	        RUN apk add curl
                sh "curl www.google.com"
            }
        }
    }
}
