pipeline {
	agent none 

		stages {
			stage {
				agent {
					docker 'maven:latest'
				}
				steps {
					sh "mvn --version"
				}
			}

			stage {
				agent {
					docker 'node:latest'
				}
				steps {
					sh "npm --version"
				}
			}
		}
}
