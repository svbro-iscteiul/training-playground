pipeline {
	agent none {

		stages {
			stage {
				agent {
					docker 'alpine:latest'
				}
				steps {
					sh "mvn --version"
				}
			}

			stage {
				agent {
					docker 'alpine:latest'
				}
				steps {
					sh "npm --version"
				}
			}
		}
	}
}
